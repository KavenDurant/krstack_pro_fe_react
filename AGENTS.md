# AGENTS
- 不允许在代码中使用 `any` 类型。
- 优先使用明确的类型、联合类型、泛型或 `unknown` 来避免丢失类型信息。
- 如需绕开类型问题，先补充类型定义或重构数据结构，而不是添加 `any`。
- 复刻 UI 时优先拆分为小组件（如工具栏、表格行渲染、状态条），避免单文件过长；新组件放在对应模块子目录内，并确保有明确的 props 类型。
- 列表顶部的“共计 N 条数据”行统一使用 `margin-bottom: 8px` 与 14px 左右字号色彩保持一致，避免各模块间间距不一致。
